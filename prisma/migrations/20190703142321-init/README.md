# Migration `20190703142321-init`

This migration has been generated by Michal Chmura at 7/3/2019, 2:23:21 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post"("id" text NOT NULL  ,"createdAt" timestamp(3) NOT NULL  ,"updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,"published" boolean NOT NULL DEFAULT false ,"title" text NOT NULL DEFAULT '' ,"content" text   ,PRIMARY KEY ("id"));

CREATE TABLE "public"."User"("id" text NOT NULL  ,"email" text NOT NULL DEFAULT '' ,"password" text NOT NULL DEFAULT '' ,"name" text   ,PRIMARY KEY ("id"));

ALTER TABLE "public"."Post" ADD COLUMN "author" text   REFERENCES "public"."User"("id");
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190703142321-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://[LOGIN]:[PASSWORD]@[SOME-AWS-HOSTNAME]eu-west-1.rds.amazonaws.com:5432/prisma2?schema=public"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+  output   = "node_modules/@generated/nexus-prisma"
+}
+
+model Post {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean
+  title     String
+  content   String?
+  author    User?
+}
+
+model User {
+  id       String  @default(cuid()) @id @unique
+  email    String  @unique
+  password String
+  name     String?
+  posts    Post[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190703142321-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190703142321-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
